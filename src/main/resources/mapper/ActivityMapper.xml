<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="marketing.activity.mapper.ActivityMapper">
    <insert id="createActivity" parameterType="marketing.activity.model.entity.Activity">
        INSERT INTO activity (name, description, start_time, end_time)
        VALUES (#{name}, #{description}, #{startTime}, #{endTime})
    </insert>
</mapper>